server:
  port: 8087

#spring:
#  application:
#    name: auth-service

  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration

  datasource:
    url: jdbc:postgresql://localhost:5432/Testing2
    username: postgres
    password: Scrumsys
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: mohamedzamil2000@gmail.com
    password: ***
    protocol: smtp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  cache:
    type: caffeine
    caffeine:
      spec: maximumSize=500,expireAfterWrite=5m

# ===============================
# JWT Configuration
# ===============================
jwt:
  secret: "c3ByaW5nLWJvb3Qtc2VjdXJpdHktand0LXR1dG9yaWFsLWppdmF0b2RvZGV2LXNwcmluZy1ib290LXNlY3VyaXR5LWp3dC10dXRvcmlhbA=="
  access-token:
    expiration: 900000        # 15 minutes
  refresh-token:
    expiration: 604800000     # 7 days

# ===============================
# External Providers
# ===============================
google:
  client-id: ***
  client-secret: ***
  redirect-uri: http://localhost:8081/api/v1/auth/google/callback

twilio:
  accountSid: **
  authToken: ***
  fromNumber: ****

# ===============================
# OTP
# ===============================
otp:
  length: 6
  expiry:
    minutes: 5

# ===============================
# User Service (Feign target)
# ===============================
user:
  service:
    url: http://localhost:8088

# ===============================
# Actuator
# ===============================
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always

# ===============================
# Logging
# ===============================
logging:
  level:
    com.scrumsys.authservice: DEBUG
    org.springframework.web: DEBUG
    feign: DEBUG
    org.springframework.cloud.openfeign: DEBUG